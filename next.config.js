/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com',
            'cdn.jsdelivr.net',
            'raw.githubusercontent.com',
            'w7.pngwing.com',
        ],
    }
};


module.exports = nextConfig;